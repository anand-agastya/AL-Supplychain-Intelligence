Instructions for the Coding Agent:
Process these task lists phase by phase.

As you complete a task, update the markdown checkbox from [ ] to [x].

For every logical grouping of tasks, create a corresponding unit test file inside the UnitTests directory at the root of the project. If tasks are simple (e.g., pure UI rendering of KPI cards and a layout), you may combine them into a single test file (e.g., UnitTests/Phase2_UI_Components.test.tsx).

Ensure all tests pass before moving to the next phase.
